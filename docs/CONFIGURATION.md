# Configuration

TBD — written during M2 once the `.env.example` is finalized.

Covers: every env var, how secrets are auto-generated by the installer (POSTGRES_PASSWORD, NEXTAUTH_SECRET, DATABASE_URL — patches v1.1 #3), and which knobs live in the admin Settings page instead of `.env`.
