# Troubleshooting

TBD — written during M6 as real issues surface during install testing.

Covers: common installer failures (Docker missing, DNS not resolved, TLS cert pending), bot run failures (RSS source unreachable, Claude API rate-limit), admin login lockout (use `scripts/reset-admin-password.sh`), and where to find logs.
