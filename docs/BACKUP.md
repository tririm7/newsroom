# Backup

TBD — written during M6 once `scripts/backup.sh` is finalized.

Covers: daily automated `pg_dump` (cron entry at 03:00 server time), manual backup invocation, restore procedure, off-server backup recommendations (rsync / rclone to S3-compatible storage — out of scope for v0.1, doc only).
